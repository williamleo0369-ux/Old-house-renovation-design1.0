services:
  - type: web
    name: quant-monitor
    env: node
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      npm install
      npm run build
    startCommand: node server.cjs
